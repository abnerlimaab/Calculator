export interface ITheme {
    colors: {
        general: {
            white: string;
        }
        background: string;
        text: string;
        button: {
            lowEmphasis: string;
            mediumEmphasis: string;
            highEmphasis: string;
        }
    }
}